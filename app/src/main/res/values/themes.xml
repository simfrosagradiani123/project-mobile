<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.CoffeeApps" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="android:statusBarColor">@android:color/white</item> <!-- Ganti warna status bar -->
        <item name="android:windowLightStatusBar">true</item> <!-- Agar ikon di status bar terlihat jelas (gelap) -->

    </style>
    <style name="CustomRatingBar" parent="Widget.AppCompat.RatingBar">
        <item name="android:minHeight">40dp</item>
        <item name="android:maxHeight">40dp</item>
    </style>
    <style name="Theme.CoffeeApps" parent="Base.Theme.CoffeeApps" />
</resources>
